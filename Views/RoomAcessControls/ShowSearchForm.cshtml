@model HospitalManagementSystem.Models.RoomAcessControl

@{
    ViewBag.Title = "ShowSearchForm";
}

<div class="form-horizontal">
    <h4>Search for Room</h4>
    <form asp-action="ShowFormResults"></form>
    <hr />

    <div class="form-group">
        <label for="SearchPhrase" , class="control-label col-md-2" }></label>
        <div class="col-md-10">
            <input name="SearchPhrase " , class="form-control" } />
        </div>
    </div>
</div>

<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        @Html.ActionLink("Search", "ShowSearchResults", "RoomAcessControl", null, new { @class = "btn btn-primary btn-lgt" })
    </div>
</div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

